  header {
  background-color: #3b4559;
  min-height: 10vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  font-size: calc(30px + 4vmin);
  color: white;
  font-weight: bolder;
}

nav {
  background-color: #64aee2;
}

nav a {
  margin-right: 20px;
  text-decoration: none;
  padding: 15px;
  height: 3px;
  justify-content: flex-end;
  text-align: left;
  color: white;
  justify-content: flex-start;
}

.food-message {
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 80px;
  height: 20vh;
  font-size: 30px;
  font-family: 'Pacifico', cursive;
  color: #ff5722;
  text-align: center;
}

img {
  display: block;
  margin-left: auto;
  margin-right: auto;
}

nav {
  text-align: right;
}

p {
  text-align: left;
}

nav {
  background-color: #c94679;
  text-align: right;
  padding: 10px;
}

nav a {
  margin-right: 20px;
  text-decoration: underline;
  color: white;
  font-weight: bold;
  font-size: 16px;
}

footer {
  background-color: #3b4559;
  align-items: center;
  height: 40px;
  justify-content: center;
  text-align: center;
  color: azure;
}

button:hover {
  background-color: green;
  color: white;
}

.DonorPage {
  display: flex;
  justify-content: center; 
  align-items: center;     
  min-height: 100vh;       
  background-image: url("/public/food.jpeg");
  background-size: cover;
  background-position: center;
  padding: 2rem;
  color: white;
}

form {
  background-color: rgba(355, 355, 355, 0.7); 
  
  padding: 2rem;
  border-radius: 20px;
  box-shadow: 0 0 25px rgba(0, 0, 0, 0.3);
  max-width: 600px;
  width: 100%;
  height:600%;
  color: black;
  font-size: 20px;;
}
.Form{
  height:18cm;
     
}
 .input-text{
  max-width: fit-content;
} 

input[type="text"],
input[type="date"],
input[type="time"],
textarea,

select {
  width: 100%;     
  max-width: 400px; 

}
legend{
    height: 42px;;
}


#about{
  text-align: center;
  font-style:inherit;
  font-size:xx-large;
  background-color:rgb(53, 52, 52);
  color:white;
  min-height: 8vh;
  justify-content: center;
  display: flex;
   

}
#img{
  width: 95%;
  height: 16cm;
}
#footer {
  background-color: #3b4559;
  align-items: center;
  height: 40px;
  justify-content: center;
  text-align: center;
  color: azure;
  font-size:x-large;
}
.p{
  font-size:0.8cm;
}
 /* 🔹 Full screen background image + soft overlay */
.signin-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;

  background-image: url("C:\Users\USER\Desktop\Aahaar\aahaar-1\public\img5.jpg"); /* your public folder image */
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;

  font-family: "Poppins", sans-serif;
  position: relative;
}

.signin-container::before {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.45); /* darker overlay for contrast */
  backdrop-filter: blur(2px);
  z-index: 0;
}

/* 🔹 Glassmorphism SignIn box */
.signin-box {
  background: rgba(255, 255, 255, 0.18);
  backdrop-filter: blur(15px);
  border: 1px solid rgba(255, 255, 255, 0.925);

  padding: 3rem 2rem;
  border-radius: 25px;
  box-shadow: 0 15px 50px rgba(0, 0, 0, 0.4);
  width: 100%;
  max-width: 420px;
  text-align: center;
  animation: fadeIn 0.8s ease-in-out;

  position: relative;
  z-index: 2;
  color: white;
}

/* 🔹 Gradient title text */
.signin-title {
  font-size: 2.5rem;
  font-weight: 800;
  margin-bottom: 2rem;
  background: linear-gradient(135deg, #f6f3f4, #f3eef0, #faf9fc);
  -webkit-background-clip: text;
  /* -webkit-text-fill-color: transparent; */
}

/* 🔹 Input groups with subtle colors */
.input-group {
  display: flex;
  align-items: center;
  margin-bottom: 1.5rem;
  border: 1.5px solid rgba(255, 255, 255, 0.784);
  border-radius: 15px;
  padding: 0.7rem 1rem;
  background: rgba(255, 255, 255, 0.1);
  transition: 0.3s ease-in-out;
}

.input-group:hover {
  border-color: #ece8ea;
}

.input-group input {
  border: none;
  outline: none;
  flex: 1;
  padding: 0.6rem;
  font-size: 1.5rem;
  background: transparent;
  color: white;
}

.input-group input::placeholder {
  color: rgba(255, 255, 255, 0.928);
}

.icon {
  width: 24px;
  height: 24px;
  margin-right: 12px;
  filter: brightness(0) invert(1); /* white icons */
}

/* 🔹 Forgot password text */
.forgot-password {
  font-size: 0.9rem;
  margin: 1rem 0 1.5rem 0;
  color: rgba(255, 255, 255, 0.85);
}

.forgot-password span {
  color: #ff7eb3;
  font-weight: 600;
  cursor: pointer;
  transition: 0.3s;
}

.forgot-password span:hover {
  text-decoration: underline;
}

/* 🔹 Gradient Button */
.submit-btn {
  background: linear-gradient(135deg, #845ec2, #ff7eb3, #ff758c);
  color: white;
  border: none;
  padding: 0.9rem 1rem;
  font-size: 1.1rem;
  border-radius: 15px;
  cursor: pointer;
  width: 100%;
  transition: 0.4s ease-in-out;
  font-weight: 800;
  letter-spacing: 0.5px;
  box-shadow: 0 5px 20px rgba(255, 126, 179, 0.4);
}

.submit-btn:hover {
  transform: scale(1.05);
  box-shadow: 0 8px 30px rgba(255, 126, 179, 0.6);
}

/* 🔹 Fade in animation */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(25px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}


/* Main container */
.signup {

  display: flex;
  justify-content: center; 
  align-items: center;     
  min-height: 100vh;       
 
  background-size: cover;
  background-position: center;
  padding: 2rem;
  





  max-width: 1800px;
  margin: 50px auto;
  padding: 30px;
  background-color: #f8f9fa;
  border-radius: 12px;
  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
   
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background-image: url("C:\Users\USER\Desktop\Aahaar\aahaar-1\public\img4.jpg");
}


.signup .Form {
  display: flex;
  flex-direction: column;

}


.signup .Field {
  border: 1px solid #ccc;
  padding: 20px;
  border-radius: 8px;
}

/* Legend styling */
.signup .Field legend {
  font-weight: bold;
  padding: 0 10px;
}


.signup label {
  display: flex;
  flex-direction: column;
  margin-bottom: 15px;
  font-size: 14px;
  color: #000000;
}



.signup input[type="text"],
.signup input[type="email"],
.signup input[type="password"],
.signup input[type="tel"],
.signup select,
.signup textarea {
  padding: 10px;
  margin-top: 5px;
  border-radius: 6px;
  border: 1px solid #ccc;
  font-size: 16px;       
  line-height: 1.5;      
  font-family: inherit;   
  box-sizing: border-box; 
}

/* Textarea styling */
.signup textarea {
  resize: vertical;
  min-height: 60px;
  font-size: 16px;       /* ensure same as input */
  line-height: 1.5;
}

.signup button {
  padding: 12px 20px;
  background-color: #28a745;
  color: white;
  font-size: 16px;
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: 0.3s;
}

.signup button:hover {
  background-color: #218838;
}


.signup h2 {
  text-align: center;
  margin-bottom: 25px;
  color: #080808;
}


@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
  
